import { FormatNumberNoCommaPipe } from './format-number-no-comma.pipe';

describe('FormatNumberNoCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberNoCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
